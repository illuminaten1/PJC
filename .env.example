# Variables d'environnement pour l'application Protection Juridique
# COPIEZ ce fichier vers .env et remplissez avec vos valeurs

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here_minimum_32_characters

# Database Configuration  
MONGODB_URI=mongodb://username:password@localhost:27017/protection-juridique?authSource=admin
MONGO_INITDB_ROOT_USERNAME=your_db_username
MONGO_INITDB_ROOT_PASSWORD=your_db_password

# Application Configuration
NODE_ENV=production
PORT=5002
SKIP_DATA_INIT=false

# Frontend Configuration
REACT_APP_API_URL=/api

# Instructions pour générer des secrets sécurisés:
# JWT_SECRET: openssl rand -base64 48
# DB_PASSWORD: openssl rand -base64 24